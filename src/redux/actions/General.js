export const GEN_INPUT_ACT = (stateName, newData) => {
  return { type: stateName, data: newData };
};

export const GEN_INPUT_DATA_ACT = (stateName, newData) => {
  return { type: stateName, data: newData };
};
